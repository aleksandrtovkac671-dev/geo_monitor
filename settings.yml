use_default_settings: true

server:
  secret_key: "$(openssl rand -hex 32)"
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false  # Отключаем rate-limit для локального использования

search:
  formats:
    - html
    - json  # ВКЛЮЧАЕМ JSON API
